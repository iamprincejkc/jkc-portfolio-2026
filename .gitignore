node_modules
.nuxt
.output
.netlify
.env
# Local stand-in for Netlify Blobs (Evently shared worlds in development)
.data
.DS_Store
dist

# Superseded by server/routes/robots.txt.ts (pending manual delete)
public/robots.txt.replaced-by-server-route
